[tool.poetry]
name = "boot-dev"
version = "0.1.0"
description = ""
authors = ["rafael salas <rafaelsalass20@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
